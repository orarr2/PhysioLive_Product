# PhysioLive VM service dependencies (slim, no torch/opencv/mediapipe).

fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6

# Retrieval stack. ChromaDB ships onnxruntime + its DefaultEmbeddingFunction
# (all-MiniLM-L6-v2 in ONNX form), so no separate sentence-transformers
# install is needed - which would drag in torch and 2 GB of CUDA wheels.
chromadb>=0.5
onnxruntime>=1.17

# LLM providers. Groq is called through httpx directly (no openai SDK
# needed) so we only pin the anthropic SDK for the optional
# COACH_PROVIDER=anthropic path. Nothing about that provider is
# defaulted at the code level - both ANTHROPIC_API_KEY and
# ANTHROPIC_MODEL must be set explicitly by the operator.
anthropic>=0.34

# HTTP client for outbound calls
requests>=2.31
httpx>=0.27

# Auth: signed JWTs for sessions, bcrypt-hashed passphrase check.
PyJWT>=2.8
bcrypt>=4.1

# Per-IP and per-user rate limits.
slowapi>=0.1.9
